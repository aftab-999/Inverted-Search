# Inverted-Search
Developed an inverted search system that creates an index mapping words to their occurrences in documents, enabling fast and efficient full-text search. The project processes input files, builds a searchable index, and retrieves relevant results based on user queries.
